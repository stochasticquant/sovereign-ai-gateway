// TODO(phase-4): Provider management admin endpoints.
// GET  /admin/providers         — list configured providers with health status
// POST /admin/providers         — register a new provider
// PUT  /admin/providers/:id     — update provider config
